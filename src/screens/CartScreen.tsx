import React from 'react';
import { Center, Text } from 'native-base';

const CartScreen = () => {
  return (
    <Center flex={1} background="background">
      <Text>Cart Screen</Text>
    </Center>
  );
};

export default CartScreen;
